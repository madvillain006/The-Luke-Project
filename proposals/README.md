# Proposals

Design sketches, mockups, and roadmaps that have not become canonical implementation docs.

Once a proposal is implemented, move the durable truth into `docs/` and leave only historical context here.
