## SWEEP: Move hardcoded Finnhub API key to environment variable
**Files:** agents/agent-06-research.js (line 17) | **Issue:** FINNHUB_KEY hardcoded as public constant in source, exposing credential and preventing rotation. | **Fix:** Replace constant with `const FINNHUB_KEY = process.env.FINNHUB_KEY;` and add to `.env`. | **Risk:** low

---

## SWEEP: Fix unhandled fetch promises in architect notifications
**Files:** agents/agent-09-architect.js (lines 261, 271) | **Issue:** Two `fetch()` calls to /notify lack await/catch, causing untracked promise rejections and resource leaks on every high-severity scan. | **Fix:** Extract `notify(payload)` helper that awaits fetch with `.catch(err => logToArchLog(err))`; replace both call sites. | **Risk:** low

---

## SWEEP: Downgrade opportunity pitch drafting from Opus to Haiku
**Files:** agents/agent-07-opportunity.js (line 119) | **Issue:** `/draft-pitch` uses claude-opus-4-6 for structured pitch text; intake endpoint already handles similar tasks on Haiku. | **Fix:** Change model string to `claude-haiku-4-5-20251001` to match intake endpoint. | **Risk:** low

---

## SWEEP: Replace Claude calls with local arithmetic for deterministic finance/income summaries
**Files:** agents/agent-03-income.js (line 115), agents/agent-05-finance.js (lines 115, 135) | **Issue:** Weekly income summary, /forecast, and /assess call Claude for simple math (averages, on-track comparisons) with no reasoning required. | **Fix:** Compute results locally; only invoke Claude when escalation thresholds hit (e.g., weeks<4, weekly_needed>1200, timeline at risk). | **Risk:** low

---

## SWEEP: Add input validation to prevent corrupted data & malformed API calls
**Files:** agents/agent-03-income.js (line 95), agents/agent-07-opportunity.js (line 136), agents/agent-03-income.js (line 128) | **Issue:** Negative `miles` accepted (inflates hourly rate); `/scan-context` accepts null/empty context; `/income-summary` doesn't guard empty `thisWeek` array. | **Fix:** Add guards: `if (miles < 0) return 400`, `if (!context?.trim()) return 400`, `if (thisWeek.length === 0) return res.json({ reply: 'No shifts this week.' })`. | **Risk:** low

---

## SWEEP: Add timeout to external market data fetches
**Files:** agents/agent-06-research.js (line 295) | **Issue:** Finnhub fetch has no timeout; slow/hanging responses block background-cycle scheduler. | **Fix:** Wrap fetch with `AbortController` + 5s timeout and explicit catch that logs and returns null. | **Risk:** low

---

## SWEEP: Cache system prompt and deduplicate history loaders in research agent
**Files:** agents/agent-06-research.js (lines 49, 107) | **Issue:** `buildAgent06System()` rebuilds 800+ token prompt per request; `loadDiscordSignals()` and `loadAllHistoricalSignals()` duplicate parse logic. | **Fix:** Cache prompt in module scope (invalidate on skill file mtime change); extract `loadHistoryFile(limit, filter)` helper used by both loaders. | **Risk:** low

---

## SWEEP: Extract shared file-save and date helpers
**Files:** agents/agent-07-opportunity.js (line 147), agents/agent-09-architect.js (lines 74, 235, 305) | **Issue:** `/update` and `/draft-pitch` both rewrite OPPS_FILE; `new Date().toISOString().slice(0,10)` repeated 3x in architect. | **Fix:** Add `saveOpportunitiesList(opps)` in opportunity agent and `getToday()` utility in architect; replace inline occurrences. | **Risk:** low

---

## SWEEP: Parallelize Sienna chunk analysis and surface truncation warnings
**Files:** agents/agent-08-sienna.js (lines 88, 108, 150) | **Issue:** Haiku loops up to 12 chunks serially with 400ms delay; synthesis truncates at 14000 chars silently; /score-signal slices profile to 1500 chars without warning. | **Fix:** Use `Promise.all()` with a small concurrency limiter (e.g. 3) for chunk analysis; log a warning whenever input is sliced; add a pre-summarization pass when combined data exceeds 14000 chars. | **Risk:** med

---

## SWEEP: Remove dead code and silent error swallowing
**Files:** agents/agent-05-finance.js (line 30), agents/agent-07-opportunity.js (line 20), agents/agent-08-sienna.js (line 42), agents/agent-09-architect.js (line 297) | **Issue:** Redundant tone guidance in AGENT_05_SYSTEM; `loadOpportunities()` swallows all errors; unused `NO_ACTIONABLE` filter; unused `REJECTED` constant. | **Fix:** Remove redundant tone line; log parse errors with file path in loadOpportunities; delete NO_ACTIONABLE filter and REJECTED constant (or implement rejection tracking). | **Risk:** low