# State Helpers

Tracked files in this folder are state helper modules, not live runtime state.

Runtime snapshots, event logs, crash logs, and local credentials are ignored. Shared Luke companion memory is stored locally at `state/snapshots/memory.json` during runtime.
